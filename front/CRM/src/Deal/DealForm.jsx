import React, { Component } from "react"; 

class InputDeal extends Component {
  render() {
    return (
     <div>
       <h1>weq</h1>
     </div>
    );
  }
}

export default InputDeal

          
{/* <input type="textarea"onChange={this.handleChangePrice("price")} value={price} />
           
<input onChange={this.handleChange("ItemOne")}  value={ItemOne} type="textarea" />


<input onChange={this.handleChange("comentDeal")}   value={comentDeal}  type="textarea" />

<span>
<div>Поставте оценку</div>
<Rate tooltips={desc} onChange={this.handleChangeRate} value={rate} />
{rate ? <span className="ant-rate-text">{desc[rate - 1]}</span> : ''}
</span> */}